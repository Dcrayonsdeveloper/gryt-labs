*.log
.DS_Store

# --- Secrets: ignore every .env variant, keep only the example template ---
.env
.env.*
!.env.example
*.env

# Backup / dumped files that often hold secrets or stale code
*.bak
*.bak-*
*.orig
*.dump
secrets.*
*credentials*

.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# /public/build is committed so Hostinger (Cloud/shared, no reliable npm) gets prebuilt assets
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
/Dcrayons.pem
/ssh/*.pem
*.pem
/.vite
/.claude
/backups
_nul
cookies.txt
/pos-desktop
/temp
*.sql
*.py
